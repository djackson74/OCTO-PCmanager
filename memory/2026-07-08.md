# 2026-07-08 — Windows Sandbox installer test failure

Host GAMER: Sandbox VM attached to Default Switch but in-Sandbox connectivity
failed during OCTO launcher installer test.

**Log signals:** Tcpip 4266 (UDP ephemeral exhaustion), WLAN 4003 (limited
connectivity), repeated LAN IP conflict (7023). Stock Default.wsb had no
mapped folders.

**Playbook:** `scripts/Test-WindowsSandboxHealth.ps1` on host; reboot and
fix LAN/IP/Wi-Fi; use mapped-folder `.wsb` for installer bootstrap.

See `docs/windows-sandbox.md` and SKILL.md § Windows Sandbox.